# app.yml
---
- hosts: pi5
  roles:
    - app

#ansible-playbook -i inventory app.yml --ask-pass --ask-become-pass
# use:
#k --kubeconfig=~/.kube/k3d-pi5-config
# OR
#export KUBECONFIG=~/.kube/k3d-pi5-config